name: fetch_url
description: "Fetch a URL and return its text content."
parameters:
  type: object
  properties:
    url:
      type: string
  required: ["url"]
mcp_server: "npx"
server_args: "-y @modelcontextprotocol/server-fetch@latest"
port: 50053
