name: Queen Orchestrator

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

env:
  MAX_WORKERS: 19
  OLLAMA_MODEL: granite4:tiny-h
  GATEWAY_PORT: 18789

jobs:
  queen:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Install Queen Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl git nodejs npm jq
          curl -fsSL https://ollama.com/install.sh | sh
          sudo npm install -g openclaw holesail
          
          # Fetch cloudflared binary directly
          curl -L -o cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared

      - name: Start Ollama Engine
        run: |
          ollama serve &
          sleep 5
          ollama pull ${{ env.OLLAMA_MODEL }}

      - name: Pre-build Architecture & Skills Layout
        run: |
          # Create custom skill workspace folder for your launcher script
          mkdir -p ~/.openclaw/workspace/skills/run_worker
          
          # Register your custom agent skill tool via markdown manifest natively
          cat << 'EOF' > ~/.openclaw/workspace/skills/run_worker/SKILL.md
          # run_worker
          
          Execute a query in an autonomous, temporary GitHub Actions worker over Holesail.
          
          ## Configuration
          - type: command
          - command: /home/runner/work/runner/runner/scripts/worker_launcher.sh
          
          ## Parameters
          - query (string): The precise task query to be executed by the worker.
          EOF

          # Enforce execution permissions on your deployment script
          chmod +x $GITHUB_WORKSPACE/scripts/worker_launcher.sh

      - name: Configure and Launch OpenClaw Natively
        run: |
          mkdir -p ~/.openclaw
          cat << EOF > ~/.openclaw/openclaw.json
          {
            "gateway": {
              "port": 18789,
              "mode": "local",
              "bind": "loopback",
              "auth": {
                "mode": "none"
              }
            },
            "agents": {
              "defaults": {
                "model": "ollama/granite4:tiny-h"
              }
            },
            "models": {
              "providers": {
                "ollama": {
                  "baseUrl": "http://127.0.0.1:11434",
                  "models": [
                    { 
                      "id": "granite4:tiny-h", 
                      "name": "granite4:tiny-h",
                      "api": "ollama" 
                    }
                  ]
                }
              }
            }
          }
          EOF

          # Completely disable origin checking via the official emergency environment flag
          export OPENCLAW_GATEWAY_CONTROL_UI_DISABLE_ORIGIN_CHECK="true"

          # Boot up the openclaw gateway engine
          nohup openclaw gateway start --allow-unconfigured > openclaw.log 2>&1 &
          
          echo "⏳ Verifying control gateway boot sequence..."
          for i in {1..20}; do
            if curl -s http://127.0.0.1:${{ env.GATEWAY_PORT }} > /dev/null || [ $? -eq 52 ]; then
              echo "✅ OpenClaw Gateway is online and listening!"
              break
            fi
            sleep 3
          done

      - name: Start Cloudflare Tunnel Connection
        id: tunnel
        run: |
          # Force IPv4 connection to the loopback interface using explicit IP routing flags
          nohup ./cloudflared tunnel --url http://127.0.0.1:${{ env.GATEWAY_PORT }} --http-host-header "127.0.0.1:${{ env.GATEWAY_PORT }}" --metrics 127.0.0.1:45455 > tunnel.log 2>&1 &
          
          echo "⏳ Catching TryCloudflare edge address registration..."
          for i in {1..20}; do
            URL=$(grep -oP 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' tunnel.log | head -1)
            if [ -n "$URL" ]; then 
              echo "tunnel_url=$URL" >> $GITHUB_OUTPUT
              echo "Found address: $URL"
              break
            fi
            sleep 3
          done
          
          # Allow edge propagation buffer time before link interaction
          sleep 5

      - name: Print Orchestration Access Route & Keep Alive
        run: |
          echo "================================================================="
          echo "🌐 OPENCLAW PORTAL ROUTE: ${{ steps.tunnel.outputs.tunnel_url }}"
          echo "================================================================="
          echo ""
          echo "--- LOG DIAGNOSTICS (OpenClaw Output Preview) ---"
          head -n 20 openclaw.log || true
          echo ""
          echo "--- LOG DIAGNOSTICS (Cloudflared Output Preview) ---"
          head -n 20 tunnel.log || true
          echo "================================================================="
          
          # Hold runner pipeline open
          sleep infinity
