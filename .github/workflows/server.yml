name: Queen Orchestrator

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

env:
  MAX_WORKERS: 19
  OLLAMA_MODEL: granite4:tiny-h
  GATEWAY_PORT: 18789

jobs:
  queen:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      # 1. Install all runtime binaries & toolsets natively
      - name: Install Queen Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl git nodejs npm jq netcat-openbsd
          curl -fsSL https://ollama.com/install.sh | sh
          sudo npm install -g openclaw holesail
          
          # Fetch cloudflared binary directly
          curl -L -o cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared

      # 2. Fire up the base model weights engine 
      - name: Start Ollama Engine
        run: |
          ollama serve &
          sleep 5
          ollama pull ${{ env.OLLAMA_MODEL }}

      # 3. Provision skills directory & launch the gateway with origin overrides
      - name: Launch OpenClaw Gateway via Ollama
        run: |
          # Create custom skill workspace folder for your launcher script
          mkdir -p ~/.openclaw/workspace/skills/run_worker
          
          # Register your custom agent skill tool via markdown manifest natively
          cat << 'EOF' > ~/.openclaw/workspace/skills/run_worker/SKILL.md
          # run_worker
          
          Execute a query in an autonomous, temporary GitHub Actions worker over Holesail.
          
          ## Configuration
          - type: command
          - command: /home/runner/work/runner/runner/scripts/worker_launcher.sh
          
          ## Parameters
          - query (string): The precise task query to be executed by the worker.
          EOF

          # Enforce execution permissions on your deployment script
          chmod +x $GITHUB_WORKSPACE/scripts/worker_launcher.sh

          # A. Run via Ollama first to assemble initial environment parameters & profiles
          ollama launch openclaw --model ${{ env.OLLAMA_MODEL }} --yes &
          sleep 12
          
          # B. Gracefully kill the profile instance to free up the target ports
          pkill -f "openclaw" || true
          sleep 3

          # C. Relaunch via pristine native binary to enforce clean global environment flags
          export OPENCLAW_GATEWAY_PORT=${{ env.GATEWAY_PORT }}
          export OPENCLAW_GATEWAY_CONTROL_UI_DISABLE_ORIGIN_CHECK="true"
          
          nohup openclaw gateway start > openclaw.log 2>&1 &
          
          echo "⏳ Waiting for OpenClaw Gateway to bind to port ${{ env.GATEWAY_PORT }}..."
          for i in {1..20}; do
            if curl -s http://127.0.0.1:${{ env.GATEWAY_PORT }} > /dev/null || [ $? -eq 52 ]; then
              echo "✅ OpenClaw Gateway successfully verified online without origin checks!"
              break
            fi
            echo "Keep waiting... ($i/20)"
            sleep 3
          done

      # 4. Bind the active upstream dynamic cloudflare proxy tunnel
      - name: Start Cloudflare Tunnel Connection
        id: tunnel
        run: |
          nohup ./cloudflared tunnel --url http://127.0.0.1:${{ env.GATEWAY_PORT }} --metrics 127.0.0.1:45455 > tunnel.log 2>&1 &
          
          echo "⏳ Catching TryCloudflare edge address registration..."
          for i in {1..20}; do
            URL=$(grep -oP 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' tunnel.log | head -1)
            if [ -n "$URL" ]; then 
              echo "tunnel_url=$URL" >> $GITHUB_OUTPUT
              echo "Found address: $URL"
              break
            fi
            sleep 3
          done
          
          # Dynamic network buffer cooldown
          sleep 5

      # 5. Output active connection string & park runner execution path
      - name: Print Orchestration Access Route & Keep Alive
        run: |
          echo "================================================================="
          echo "🌐 OPENCLAW PORTAL ROUTE: ${{ steps.tunnel.outputs.tunnel_url }}"
          echo "================================================================="
          echo ""
          echo "--- LOG DIAGNOSTICS (OpenClaw Output Preview) ---"
          head -n 40 openclaw.log || true
          echo ""
          echo "--- LOG DIAGNOSTICS (Cloudflared Output Preview) ---"
          head -n 20 tunnel.log || true
          echo "================================================================="
          
          # Hold runner pipeline open
          sleep infinity
